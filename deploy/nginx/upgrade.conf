# Maps the Upgrade request header to the Connection header nginx should send
# upstream — required for the Blazor Server WebSocket circuit.
# Install to /etc/nginx/conf.d/upgrade.conf (http-level context).
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
